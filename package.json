{
  "name": "openclaw-live-lens",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Live timing lens for OpenClaw agent turns and plugin spans",
  "type": "module",
  "main": "dist/index.js",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "runtimeExtensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "openclaw": "file:../openclaw",
    "typescript": "^5.0.0"
  }
}
